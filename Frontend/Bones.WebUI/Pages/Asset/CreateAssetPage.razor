@using Bones.WebUI.Models
@using Bones.WebUI.Components.Item
@attribute [Route(FrontEndUrls.Asset.CREATE_ASSET)]

<MudText Typo="Typo.h4">Create Asset</MudText>

<MudGrid>
    <MudItem xs="12" sm="7">
        <MudPaper Class="pa-4">
            @if (!AssetLayoutIdProvidedInQueryString)
            {
                <MudSelect ValueChanged="OnSelectedProjectChanged"
                           Label="Select Project"
                           MultiSelection="false"
                           FullWidth="true"
                           T="Guid?">

                    <MudSelectItem Value="@((Guid?)null)" Disabled="true" Style="height: 1px; margin: 0; padding: 0;"></MudSelectItem>
                    @foreach (DropDownModel proj in Projects)
                    {
                        <MudSelectItem Value="@proj.Id">@proj.ToString()</MudSelectItem>
                    }
                </MudSelect>

                if (SelectedProject != null)
                {
                    <MudSelect ValueChanged="OnSelectedLayoutChanged"
                               Label="Select Asset Layout"
                               MultiSelection="false"
                               FullWidth="true"
                               T="Guid?">

                        <MudSelectItem Value="@((Guid?)null)" Disabled="true" Style="height: 1px; margin: 0; padding: 0;"></MudSelectItem>
                        @foreach (DropDownModel itemLayout in AssetLayouts)
                        {
                            <MudSelectItem Value="@itemLayout.Id">@itemLayout.ToString()</MudSelectItem>
                        }
                    </MudSelect>
                }
            }
            else
            {
                <MudTextField T="string" Label="Asset Layout Name" Disabled @bind-Value="@AssetLayoutName"/>
            }
        </MudPaper>
        <br />
        <br />
        @if(SelectedAssetLayout != null)
        {
            <MudPaper Class="pa-4">
                <ItemEditor ItemId="@null" ItemType="ItemLayoutUse.Assets" EditorMode="ItemEditor.Mode.Create" ItemLayoutId="@SelectedAssetLayout.Value" />
            </MudPaper>
        }
    </MudItem>
    <MudItem xs="12" sm="5">
        <MudItem Class="pa-2" hidden="@(!ApiError)">
            <MudAlert Severity="Severity.Error">There was an error with the creation, please try again.</MudAlert>
        </MudItem>

        @foreach (string error in ValidationErrors)
        {
            <MudItem Class="pa-2">
                <MudAlert  Severity="Severity.Error">@error</MudAlert>
            </MudItem>
        }
    </MudItem>
</MudGrid>