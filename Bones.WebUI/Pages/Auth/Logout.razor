@page "/Auth/Logout"
<h3>Logout</h3>

<p>Logging you out now...</p>

@code {
    protected override async Task OnInitializedAsync()
    {
        await ApiClient.AuthLogoutAsync();
        await AuthStateProvider.SetCurrentUserAsync(null);

        NavManager.NavigateTo("/");
    }
}